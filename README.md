Database Stuff:

# Create a Table
c.execute("""CREATE TABLE word (
            word text,
        definition text,
        synonym text
    )""")

# Create a Table
c.execute("""INSERT INTO word VALUES ()""")